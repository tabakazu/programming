# dining_philosophers - Rust Tutorial

## Use Docker
```bash
$ docker-compose build
$ docker-compose run --rm --service-ports app bash
```

## Create a new project
```bash
docker > cargo init --bin
```

## Run app
```bash
docker > cargo run
```
